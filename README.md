# 作業小記
---
## root
- 將每次作業的代碼以物件陣列獨立存放在js裡，避免html過於冗長。
- 使用js動態生成每個作業的標籤頁(將每次作業都視為單一一個元件)，避免html過於冗長，同時便於切換瀏覽。
---
- 使用按鈕監聽與布林值來觸發字元與字元代碼之間的切換來達到雙檢視模式。
## 9月9日-來來小吃店
- 使用`<caption>`作為表格標題，有助於提升語意化程度。
- 以`<tbody>`劃分區塊，有助於提升語意化程度。
---
## 9月18日-個人報名表單
- 將死資料獨立存放於js檔，再經由`<script>`引入，避免html過於冗長。
- 有固定邏輯可推演的資料使用迴圈與方法動態生成，同避免死資料過於冗長。
- 將高度重複的架構改採js方法動態注入，避免html過於冗長。
- 按功能與負責區塊拆解js/css，避免單一js/css的功能過於複雜或冗長。
- 行政區劃採用物件陣列存放，並使用js動態判定注入。使用者必須先選擇縣市，等縣市有值之後再根據這個值去物件陣列翻出對應的鄉鎮市區陣列，再將這個陣列轉換成DOM注入父元素中並渲染出來。
- 出生日期的判定也採取類似的方式。使用者必須先輸入年和月，才會根據年和月的性質注入對應的日選項DOM並渲染出來。
- 還沒做驗證輸入(input.value是否合規等)。